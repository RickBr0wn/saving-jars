import React from 'react'

const Title = () => {
  const styles = {
    marginTop: '1.25rem',
    marginBottom: '2.25rem'
  }

  return (
    <h2 style={styles}>saving jars</h2>
  )
}

export default Title 